---

spring:
  profiles:
    active: production
  application:
    name: cruncher

---

spring:
    profiles: development

server:
    port: 9001

github:
  enabled: true
  baseUrl: http://github.com
  accessToken: accessMeow
---

spring:
    profiles: production
    main:
        banner-mode: "off"

project:
  name: bluenode
github:
  enabled: true
  baseUrl: 0
  accessToken: accessMeow